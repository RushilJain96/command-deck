"use client";

/**
 * Placeholder shell. Sprint 1's acceptance criteria forbid portfolio content,
 * and the launch/warp transition that reaches this scene is Sprint 3 — this
 * exists so the scene registry has a third member and the union is exercised.
 */
export function ProjectScene() {
  return (
    <div className="flex h-full w-full items-center justify-center">
      <p className="text-t3 font-mono text-xs tracking-[0.35em] uppercase">
        Project Scene
      </p>
    </div>
  );
}
